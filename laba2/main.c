#include <stdio.h>
#include "header.h"                         //падключыць файл з прататыпамі функцый.
int main(void)                              
{
    int arr[N];                             //дэкларацыя масіву.
    input(arr);                             //выклік функцыі ўводу.
    sort(arr);                              //выклік функцыі сартыроўкі.
    output(arr);                            //выклік функцыі вываду.
    return 0;
}